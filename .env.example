# Webshare Proxy Configuration (optional)
# Get these values from your Webshare dashboard
WEBSHARE_USERNAME=your_username_here
WEBSHARE_PASSWORD=your_password_here

# Optional: Filter proxy IPs by country (comma-separated)
# Examples: "us,de" for USA and Germany, "us" for USA only
# Leave empty to use all available locations
WEBSHARE_COUNTRIES=

# Rate Limiting Configuration
# Redis connection string (required for rate limiting)
# Example: rediss://default:password@host:port
REDIS_URL=your_redis_connection_string_here

# Daily API request limit per IP address
DAILY_LIMIT=5

# Admin token for bypass management (generate a long random string)
# Example: openssl rand -base64 32
ADMIN_TOKEN=your_long_random_admin_token_here

# Flask Configuration
FLASK_ENV=production